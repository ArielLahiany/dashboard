apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashboard
  namespace: saleor
  labels:
    app: saleor
    component: dashboard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
